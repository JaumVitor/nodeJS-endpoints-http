<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head'); %>
  <body>
    <%- include('partials/header') %>
    <div class="container">
      <% if (users.length > 0) { users.forEach(user => {%>
      <div class="post">
        <h3><%= user.firtName %> <%= user.lastName %></h3>
        <p><%= user.email %></p>
      </div>
      <% }) %> <% } %>
    </div>
  </body>
  <%- include('partials/footer') %>
</html>
